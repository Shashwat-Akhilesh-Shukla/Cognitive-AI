# Core Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6

# CORS and middleware
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.12.0

# Redis and Caching
redis>=5.0.0

# Authentication
pyjwt>=2.8.0
bcrypt>=4.1.0

# HTTP Client
httpx>=0.25.0

# Vector Database (Pinecone)
pinecone-client>=2.2.0

# PDF Processing
pdfplumber>=0.10.0

# AI/ML Libraries (PyTorch installed separately via install_pytorch.sh)
# torch - installed via install_pytorch.sh
# torchaudio - installed via install_pytorch.sh

# ============================================
# GPU/ML PACKAGES (OPTIONAL - Uncomment if you have GPU and need voice/ML features)
# ============================================
# These packages are VERY large and slow to download. Only enable if needed:
# - transformers: 500MB+ with dependencies (for NLP models)
# - TTS: 200MB+ with dependencies (for text-to-speech)
# - sentence-transformers: 300MB+ (for embeddings/RAG)
# - librosa: 100MB+ (for audio processing)
#
# transformers>=4.35.0
# TTS>=0.20.0
# sentence-transformers>=2.2.0
# librosa>=0.10.0

# Audio Processing (lightweight, keep these)
pydub>=0.25.0
soundfile>=0.12.0
numpy>=1.24.0,<2.0.0

# Utilities
python-dateutil>=2.8.0


fastapi
uvicorn[standard]

httpx
requests

pydantic
python-dotenv
python-multipart

bcrypt
PyJWT

redis
pinecone

numpy>=1.24.3,<2.0  # Compatible with both TTS and faster-whisper
pdfplumber

# Voice Processing Dependencies
faster-whisper>=0.10.0

# TTS and Dependencies - Not compatible with Python 3.12
TTS>=0.22.0
gruut>=2.2.3
gruut-ipa>=0.13.0
bnnumerizer>=0.0.2
jieba>=0.42.1
pydub>=0.25.1
soundfile>=0.12.1

# Note: PyTorch is installed separately via install_pytorch.sh
# This allows conditional installation based on GPU availability

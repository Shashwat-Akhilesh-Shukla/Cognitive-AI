fastapi
uvicorn

httpx
requests

pydantic
python-dotenv
python-multipart

bcrypt
PyJWT

redis
pinecone

numpy
pdfplumber

# Voice Processing Dependencies
faster-whisper>=0.10.0
# TTS - Not compatible with Python 3.12, install manually if needed with older Python
pydub>=0.25.1
soundfile>=0.12.1

# PyTorch CPU-only (no NVIDIA GPU) - Install separately with: 
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu